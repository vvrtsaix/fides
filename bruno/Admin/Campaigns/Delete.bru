meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{admin}}/admin/campaigns/{{campaign_id}}
  body: none
  auth: none
}

headers {
  X-Tenant-Id: {{tenant_id}}
}

docs {
  Soft delete: sets active = false.
}
