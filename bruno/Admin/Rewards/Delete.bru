meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{admin}}/admin/rewards/{{reward_id}}
  body: none
  auth: none
}

headers {
  X-Tenant-Id: {{tenant_id}}
}

docs {
  Soft delete: sets active = false. ponytail: redemption does not yet check active.
}
